#include <stdio.h>
#include "id_lookup_table_test.h"
//#include "id_lookup_table.h"

void id_lookup_table_test()
{
  /*
  int i;
  const int ITEM_COUNT = 10;
  ID public_ids[ITEM_COUNT];

  id_lookup_t id_lookup_t;
  id_lookup_t_init(&id_lookup_t, sizeof(int), ITEM_COUNT);
  
  printf ("Begin add %d elements\n", ITEM_COUNT);
  for (i = 0; i < ITEM_COUNT; i++)
  {
    public_ids[i] = id_lookup_t_add(&id_lookup_t);
    printf ("   public_ids[%d] = %X\n", i, public_ids[i]);
  }
  printf ("Add ended.\n");

  printf ("Begin remove\n");
  for (i = 0; i < ITEM_COUNT; i++)
  {
    if(id_lookup_t_has(&id_lookup_t, public_ids[i]))
    {
      id_lookup_t_remove(&id_lookup_t, public_ids[i]);
      printf ("   %x removed\n", public_ids[i]);
    }
    else
    {
      printf ("   %x not found\n", public_ids[i]);
    }
  }
  printf ("Remove end\n");
  */
}